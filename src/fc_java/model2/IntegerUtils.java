package fc_java.model2;

public class IntegerUtils {
    // static -> 정적 메서드, 클래스 메서드
    public static int stringToInt(String S){
        return Integer.parseInt(S); // "100" -> 100 문자열을 정수형으로 바꿔 준다.
    }
}
